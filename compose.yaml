services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=TeamManagementSystem'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=root'
    ports:
      - '5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
